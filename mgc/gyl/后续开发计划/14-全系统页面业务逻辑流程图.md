# 全系统页面业务逻辑流程图

更新时间：2026-04-13  
覆盖范围：`src/router/index.ts` 中已挂载页面 + `src/views/*.vue` 对应业务链路

## 1. 页面分层总览（全系统）

```mermaid
flowchart LR
    A["认证与访问层\nLogin/ForgotPassword/Profile"] --> B["组织权限层\nDepartment/User/Role/Post/Permission/Dict"]
    B --> C["平台治理层\nOperationLog + Platform* + Import/Export"]
    B --> D["主数据管理层\nMdm* + Governance"]
    D --> E["业务运营层\nOrderClosedLoop/InventoryOps/ChannelDealerOps/Pasture"]
    E --> F["经营分析层\nManagementCockpit"]
    C --> F
    C --> G["流程协同层\nWorkflowCenter"]
    E --> G
    D --> G
```

## 2. 全页面与数据域关系图

```mermaid
flowchart TB
    subgraph SYS["system 域（组织权限）"]
        s1["DepartmentManage"]
        s2["UserManage"]
        s3["RoleManage"]
        s4["PostManage"]
        s5["PermissionManage"]
        s6["DictCenter"]
    end

    subgraph MDM["master 域（主数据）"]
        m1["MdmSkuList"]
        m2["MdmResellerRelation"]
        m3["MdmCategoryTree"]
        m4["MdmWarehouseList"]
        m5["MdmFactoryList"]
        m6["MdmChannelTree"]
        m7["MdmResellerList"]
        m8["MdmOrgTree"]
        m9["MdmCalendar"]
        m10["MdmRltnWarehouseSku"]
        m11["MdmRltnOrgReseller"]
        m12["MdmRltnProductSku"]
        m13["MdmGovernanceCenter"]
    end

    subgraph BIZ["biz 域（业务）"]
        b1["OrderClosedLoopCenter"]
        b2["InventoryOpsCenter"]
        b3["ChannelDealerOpsCenter"]
        b4["PastureOverview"]
        b5["IntelligentOrdering（旧链路只读）"]
    end

    subgraph PLT["platform 域（平台）"]
        p1["OperationLogCenter"]
        p2["ImportTaskCenter"]
        p3["ExportTaskCenter"]
        p4["PlatformAuditLogPage"]
        p5["PlatformSecurityCenterPage"]
        p6["PlatformConfigCenterPage"]
        p7["PlatformArchiveStrategyPage"]
        p8["PlatformMonitorPage"]
        p9["PlatformFinePermissionPage"]
        p10["PlatformHealthViewPage"]
        p11["WorkflowCenter"]
    end

    subgraph ANA["分析域"]
        a1["ManagementCockpit"]
    end

    SYS --> MDM
    MDM --> BIZ
    BIZ --> ANA
    PLT --> ANA
    BIZ --> PLT
    MDM --> PLT
```

## 3. 订单创建与多页面同步（核心链路）

> 订单创建入口：`/intelligent-closed-loop`（`OrderClosedLoopCenter.vue`）

```mermaid
sequenceDiagram
    autonumber
    participant U as 业务用户
    participant OC as OrderClosedLoopCenter
    participant API as orderPhase2 API
    participant BIZ as biz.order_headers/order_lines
    participant INV as biz.inventory_*（ledger/locks/tx/warnings）
    participant CH as ChannelDealerOpsCenter
    participant MC as ManagementCockpit
    participant LOG as OperationLog + AuditLog
    participant MON as PlatformMonitor

    U->>OC: 新建订单（客户/区域/SKU/数量/期望到货）
    OC->>API: POST /api/orders/phase2
    API->>BIZ: 写入订单头+订单行
    API->>LOG: 记录CREATE操作日志/审计日志
    API->>MON: 记录接口调用指标
    API-->>OC: 返回orderNo与初始状态

    OC->>API: 提交审核/审核通过/驳回/复制
    API->>BIZ: 更新订单状态与审核轨迹
    API->>INV: 分配时冻结库存，驳回/关闭时释放库存

    BIZ-->>CH: 同步订单事实（渠道销量、经销排名、越权扫描）
    BIZ-->>MC: 同步订单分析（订单量/金额/区域渠道分布/履约）
    INV-->>MC: 同步库存健康（可用量、锁定量、预警数）
    LOG-->>U: 在操作日志与审计日志页可追溯
```

### 3.1 订单动作 -> 联动页面 -> 更新显示内容

| 订单动作 | 触发页面 | 同步页面 | 主要更新显示内容 |
|---|---|---|---|
| 新建订单 | `OrderClosedLoopCenter` | `OrderClosedLoopCenter`、`ManagementCockpit`、`ChannelDealerOpsCenter`、`OperationLogCenter`、`PlatformAuditLogPage`、`PlatformMonitorPage` | 新订单号、客户/区域/SKU明细、订单总量/金额、渠道销量贡献、审计记录、API调用统计 |
| 提交审核/审核 | `OrderClosedLoopCenter` | `OrderClosedLoopCenter`、`ManagementCockpit`、`OperationLogCenter` | 状态流转（草稿→待审→待分配/驳回）、状态分布指标、操作轨迹 |
| 自动/人工分配 | `OrderClosedLoopCenter` | `InventoryOpsCenter`、`ManagementCockpit`、`OrderClosedLoopCenter` | 库存锁定量、可用量变化、分配成功率、异常类型（库存不足/效期风险） |
| 履约流转/关闭 | `OrderClosedLoopCenter` | `InventoryOpsCenter`、`ManagementCockpit`、`ChannelDealerOpsCenter` | 锁释放、履约率、渠道到货与履约表现 |

## 4. MDM 主数据变更传播图（覆盖 MDM 全页面）

```mermaid
flowchart LR
    A["MdmSkuList/MdmCategoryTree\nMdmWarehouseList/MdmFactoryList\nMdmChannelTree/MdmResellerList\nMdmOrgTree/MdmCalendar\nMdmRltn* + MdmGovernanceCenter"] --> B["master 主表与关系表更新"]
    B --> C["OrderClosedLoopCenter 配置与校验\n客户/SKU/仓库候选、授权校验"]
    B --> D["InventoryOpsCenter 仓能力/仓-SKU可达性"]
    B --> E["ChannelDealerOpsCenter 渠道/经销档案授权与价格规则"]
    B --> F["ManagementCockpit MDM质量分与经营分析维度"]
    B --> G["ImportTaskCenter/ExportTaskCenter 任务结果校验"]
```

## 5. 渠道经营与订单事实联动图

```mermaid
flowchart TB
    O["OrderClosedLoopCenter\n订单事实(order_headers/order_lines)"] --> C1["ChannelDealerOpsCenter Dashboard\n渠道销量/贡献度/区域分布"]
    O --> C2["ChannelDealerOpsCenter Analysis\n经销商排行/增长"]
    O --> C3["ChannelDealerOpsCenter Risks\n越权销售/合同临期/价格异常"]
    C1 --> M["ManagementCockpit 渠道分析看板"]
    C2 --> M
    C3 --> W["WorkflowCenter 风险跟进任务"]
```

## 6. 导入导出、通知、工作流联动图

```mermaid
flowchart LR
    I["ImportTaskCenter\n导入任务"] --> P["platform.import_tasks"]
    E["ExportTaskCenter\n导出任务"] --> Q["platform.export_tasks"]
    P --> N["notifications 未读提醒"]
    Q --> N
    P --> W["WorkflowCenter 任务汇总"]
    Q --> W
    P --> A["PlatformAuditLogPage/OperationLogCenter"]
    Q --> A
```

## 7. 权限与菜单生效链路（全页面访问控制）

```mermaid
flowchart LR
    R["RoleManage/PermissionManage\nPlatformFinePermissionPage"] --> P["角色权限与细粒度权限变更"]
    P --> M["/me 返回 permissionPaths/dataScopes"]
    M --> S["侧边栏菜单显隐 + 路由守卫"]
    S --> V["各业务页面可见性与可操作按钮"]
    P --> L["PlatformSecurityCenterPage + AuditLog 留痕"]
```

## 8. 全系统回归建议（按流程验收）

1. 订单链路：`新建 -> 提交 -> 审核 -> 分配 -> 履约/关闭`，每一步核对订单页、库存页、渠道页、驾驶舱、日志页。  
2. 主数据链路：在任一 `Mdm*` 页面改动关键主数据后，验证订单配置候选、库存可达性、渠道授权与驾驶舱维度是否同步。  
3. 平台链路：触发导入/导出任务后，核对任务中心、通知、工作流、审计日志、监控是否一致。  
4. 权限链路：调整角色权限后，验证菜单、路由、按钮权限及安全审计日志一致性。
